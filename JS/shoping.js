

const asignarAccion = () =>{

    const botones = document.querySelectorAll('button');
    
    botones.forEach(Element => {
    
        Element.addEventListener('click', ()=>{

            alert("Muchas gracias, su compra será procesada en un momento")
    
    
    
        });
        
    });
    
    }

    asignarAccion();